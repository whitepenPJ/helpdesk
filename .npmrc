# vitest 5 declares a peerOptional on @types/node ^22 || >=24, which
# conflicts with this project's pinned @types/node ^20. It's optional and
# vitest works fine without it, so tell npm to use the pre-v7 peer
# resolution instead of erroring (Vercel's `npm install` has no CLI flag).
legacy-peer-deps=true
