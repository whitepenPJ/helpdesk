// Simple on/off switches for features that are built but temporarily
// turned off — flip back to `true` to re-enable, no code removal needed.
export const CHAT_ENABLED = false;
export const PUSH_NOTIFICATIONS_ENABLED = false;
export const TEAMS_NOTIFICATIONS_ENABLED = false;
