// Test stub for the `server-only` package. The real module throws when
// evaluated outside a React Server Component graph (e.g. under Vitest);
// aliased in vitest.config.ts so server-tagged lib modules import cleanly.
export {};
